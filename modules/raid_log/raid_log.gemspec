Gem::Specification.new do |s|
  s.name        = "raid_log"
  s.version     = "1.0.0"
  s.authors     = ["OpenProject GmbH"]
  s.summary     = "OpenProject RAID Log (Risk/Issue/Action/Decision)."

  s.files = Dir["{app,config,db,lib}/**/*"]
  s.metadata["rubygems_mfa_required"] = "true"
end
